<?php

/**
 * @file
 * Contains decoupled_kit_sitemap.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function decoupled_kit_sitemap_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the decoupled_kit_sitemap module.
    case 'help.page.decoupled_kit_sitemap':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Decoupled sitemap.') . '</p>';
      return $output;

    default:
  }
}
