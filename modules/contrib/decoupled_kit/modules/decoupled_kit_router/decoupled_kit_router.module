<?php

/**
 * @file
 * Contains decoupled_kit_router.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function decoupled_kit_router_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the decoupled_kit_router module.
    case 'help.page.decoupled_kit_router':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Decoupled Kit Router get router link from own entity for Decoupled Kit Object.') . '</p>';
      return $output;

    default:
  }
}
